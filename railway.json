{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd front && npm install && npm run production-build && cd .."
  },
  "deploy": {
    "startCommand": "SS_ENV=production SS_PACK=1 node app.js"
  }
}
